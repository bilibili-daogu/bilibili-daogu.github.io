#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/E:/blog/sci-fi-blog/node_modules/.store/module-deps@3.9.1/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/E:/blog/sci-fi-blog/node_modules/.store/module-deps@3.9.1/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../module-deps@3.9.1/node_modules/module-deps/bin/cmd.js" "$@"
else
  exec node  "$basedir/../../../../../module-deps@3.9.1/node_modules/module-deps/bin/cmd.js" "$@"
fi
